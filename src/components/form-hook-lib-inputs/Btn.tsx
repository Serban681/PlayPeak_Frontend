// import { Children, ReactNode } from "react";

// const Btn = ({children, submit=false, handleClick, customStyles}: {children: ReactNode, submit?: boolean, handleClick?: (e: React.FormEvent) => void, customStyles?: string}) => {
//     return (
//         <button className={`${customStyles} border-2 p-1`} type={submit ? "submit" : "button"} onClick={handleClick}>{children}</button>
//     );
// }

// export default Btn;
